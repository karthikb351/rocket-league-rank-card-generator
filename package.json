{
  "name": "rocket-league-rank-card-generator",
  "version": "0.0.1",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "start": "functions-framework --source=build/src/ --target=app",
    "deploy": "rm -rf build && npm run compile && gcloud functions deploy app --runtime nodejs14 --trigger-http --allow-unauthenticated --project discord-rocket-league-bot",
    "watch": "concurrently \"tsc -w\" \"nodemon --watch ./build/ --exec npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^1.9.0",
    "axios": "^0.21.1",
    "chrome-aws-lambda": "^10.1.0",
    "express": "^4.17.1",
    "install": "^0.13.0",
    "mustache": "^4.2.0",
    "puppeteer": "^10.1.0",
    "puppeteer-core": "^10.1.0",
    "@types/express": "^4.17.13",
    "@types/mustache": "^4.1.2",
    "@types/node": "^14.11.2",
    "concurrently": "^6.2.0",
    "gts": "^3.1.0",
    "nodemon": "^2.0.12",
    "typescript": "^4.0.3"
  }
}
